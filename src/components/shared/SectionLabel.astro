---
interface Props {
  text: string;
  dark?: boolean;
}
const { text, dark = false } = Astro.props;
---
<span
  class:list={[
    "mb-4 inline-block text-xs font-semibold uppercase tracking-[0.2em]",
    dark ? "text-emerald-400" : "text-accent",
  ]}
>
  {text}
</span>
